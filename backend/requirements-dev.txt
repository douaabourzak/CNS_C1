# Lightweight test-only deps — no insightface / onnxruntime / opencv needed
# because all tests run with MOCK_AI=true (lazy imports are never triggered).
fastapi>=0.110
pydantic>=2.6
python-multipart>=0.0.9
python-dotenv>=1.0
numpy>=1.26
supabase>=2.4
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
