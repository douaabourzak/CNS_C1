#!/bin/sh
# Run TypeScript type-check before every commit.
# Activated automatically after `npm install` (husky v9).
cd "$(git rev-parse --show-toplevel)/frontend"
echo "Running tsc..."
npx tsc --noEmit
